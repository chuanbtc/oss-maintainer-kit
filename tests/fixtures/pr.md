# Add release command

This PR adds a release notes generator and changes the CLI parser.

## Testing

- Added unit tests
- Manually ran the command against a changelog

## Risk

Touches command routing and file IO.
